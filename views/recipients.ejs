<!DOCTYPE html>
<html>

<head>
  <%- include('./partials/head'); %>
  <script type="text/javascript" src="/js/dynamicForm.js"></script>

</head>

<body>

  <%- include('./partials/nav'); %>

  <form id="recipientsForm" action="/recipients?recipientId=<%= recipients.recipientId %>" method="POST" target="frame">
    <button class=" border-2 rounded-full py-2 px-4 " type="button" onclick="addElement(['mail', 'name'])">+</button>
    <label for="ablauf" id="input">Empfänger
      <% if(typeof recipients.name != "undefined" || typeof recipients.email != "undefined") { 
      for(i=0; i<recipients.mail.length; i++) { %>
      <div>
        <input type="text" name="mail" value="<%= recipients.mail[i] %>" placeholder="Mail">
        <input type="text" name="name" value="<%= recipients.name[i] %>" placeholder="Name">
        <button class=" removeButton border-2 rounded-full py-2 px-4 " type="button">-</button>
      </div>
      <% }} %>
    </label>
    <button class=" border-2 rounded-full py-2 px-4 " type="submit" id="save" onclick="confirmSave()">Speichern</button>
  </form>

</body>
<footer>
  <%- include('./partials/footer'); %>
</footer>

</html>