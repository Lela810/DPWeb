<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('./partials/head'); %>
  <script type="text/javascript" src="/js/confirmSave.js"></script>
</head>

<body>

  <%- include('./partials/nav'); %>

  <h3 class=" p-5 font-bold text-2xl text-center">
    Kinder Abmelden
  </h3>

  <% for(let i = 0; i < recipients.length; i++) { %>
  <form action="/invite" method="POST" target="frame">
    <input type="text" name="name" value="<%= recipients[i].name %>" readonly>
    <input type="text" name="mail" value="<%= recipients[i].mail %>" readonly>
    <input type="text" name="mailId" value="<%= mailId %>" readonly hidden>
    <button class=" border-2 rounded-full py-2 px-4 " type="submit" id="save" onclick="confirmSave()">Abmelden</button>
  </form>
  <% } %>



</body>
<footer>
  <%- include('./partials/footer'); %>
</footer>

</html>